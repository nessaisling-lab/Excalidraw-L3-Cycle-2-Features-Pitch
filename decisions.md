# decisions.md — Persistent Save (C1 First-Save Moment)

Running log of every decision made on this build: what was decided, who made the call, and why. This is the team's working reference — kept in the repo, not the PRD, so it's easy to grep and diff alongside the code it governs.

The PRD (`persistent-save-master-prd.md`) states final requirements only. This file is where the reasoning and history live. When the two ever seem to disagree, the PRD wins for what to build; this file explains why it says what it says.

**Maintainers:** whole team. Append, don't rewrite history — if a decision changes, add a new row noting the supersession rather than editing the old one.

---

## Decisions

| Date | Decision | Made by | Reasoning |
| :-- | :-- | :-- | :-- |
| 2026-09-15 | C1 First-Save Moment approved as the primary build (over candidates A and C2) | Carlos, in client meeting | Directly addresses the confirmed save-guidance gap |
| 2026-09-15 | All three surfaces (prompt, button, tooltips) approved as the final solution, staged | Carlos, in client meeting | Team had already proposed staging; Carlos signed off on the full set |
| 2026-09-15 | Persistent save button built as always-visible from landing, not conditionally triggered | Team | Simpler mental model; permanent fallback independent of the prompt's one-time firing |
| 2026-09-15 | Anonymous/non-Plus users only, this cycle | Carlos | "Free canvas, this quarter, full stop" |
| 2026-09-15 | Config-constant approach for dwell time, session gap, and quiet gate, with dev/QA URL overrides | Team | Unblocks build while these values remain unsettled; one-line change once finalized |
| 2026-09-15/16 | Switching browsers = new visitor/session | Carlos (informed the reasoning) + team (made the call) | Local browser storage doesn't carry over across browsers, consistent with the activation metric's own anonymous-session definition |
| 2026-09-16 | Dwell time revised from ~30s toward ~15s, treated as tunable not fixed (later finalized — see below) | Team | 30s could fire after some users in the 60–90s drop-off window have already left |
| 2026-09-16 | Attribution scope narrowed to bundle-level; entry-point/last-touch attribution downgraded to a cheap-to-add optional field | Team, based on Carlos's own words | Carlos's stated ask ("whether we had implemented your feature or we didn't") is bundle-level only, not a request for component-level breakdown |
| 2026-09-16 | File-save-first / Excalidraw+-second prompt ordering confirmed, with two conditions | Carlos, written reply | Plus link must stay visually subordinate (no competing CTA); dropped from the prompt without debate if test data shows cannibalization of the save action. **Superseded 2026-09-18 — see below: the link is excluded from the prompt entirely, not just reordered** |
| 2026-09-16 | Mobile/narrow-viewport support confirmed out of scope this cycle | Carlos, written reply | Scoped activation problem is overwhelmingly desktop traffic; mobile canvas is a different product problem with its own interaction model. Only requirement: prompt must not visibly break on narrow viewports (graceful suppression acceptable) |
| 2026-09-16 | Collapsible legend confirmed approved but lowest priority (P1), excluded from this PRD's requirements | Carlos, written reply | Build sequence: prompt → persistent button → tooltips → legend last. Legend is "discoverability polish," not to be spec'd in detail until the other three are locked |
| 2026-09-16 | Measurement/instrumentation plan added as a required part of the C1 prototype deliverable, not a follow-up | Carlos, written reply | Carlos needs save/export event instrumentation, A/B split mechanics for anonymous sessions, and sample size to detect the 3pp lift, to pitch his board — part of the deliverable, not trailing documentation |
| 2026-09-16 | Dwell finalized at 15s; clock pauses on backgrounded tabs, menus, and dialogs; runs during active drawing (held by a 2s quiet gate) | Team, at engineering sync | Wall-clock timers are unreliable in backgrounded tabs; app already listens for `visibilitychange`/`blur`/`focus`, so pausing costs nothing new. **Flagged to Carlos**, who said ~30s — not yet re-confirmed by him at the new value. **Still not re-confirmed as of 2026-09-18 — see below. Confirmed by Carlos later on 2026-09-18 — see final dwell row below** |
| 2026-09-16 | Qualifying element finalized: ≥10 scene px (width or height), or any text regardless of size; library-inserted elements count | Team, at engineering sync | Confirms and finalizes the earlier "minimum bounding-box size" lean from the 9/16 poll |
| 2026-09-16 | Undo to empty canvas resets prompt eligibility to not-yet-fired and restores the welcome layer | Team, at engineering sync | The app's welcome layer already re-shows whenever the canvas empties; no new logic required, just confirming the behavior is intended |
| 2026-09-16 | Persistent save button: click saves directly to file (same action as Ctrl/Cmd+S); does not open the three-option Save dialog | Team, at engineering sync | The button exists to remove friction, not add a step. Save/export/share stay reachable from the existing menu |
| 2026-09-16 | Persistent save button confirmed as a **permanent product surface**, not prototype-only, and built to production standards | Carlos, via Aisling/team sync | Team asked Carlos directly; he confirmed permanent. Because the button sits behind its own switch, the engineering effort is the same either way, so this didn't block starting the build. **Superseded 2026-09-17 — see below** |
| 2026-09-17 | **Supersedes 2026-09-16 entry above.** Persistent save button is **prototype quality this cycle**, not production standard. Permanence decision deferred to next cycle, contingent on A/B results | Carlos, written reply to Jillian's direct question | "Everything in this cycle exists to prove the activation lift; nothing gets built to production standards until the A/B data says it earned a permanent spot." Build bar: reliable enough that test users don't hit bugs that contaminate activation data; settings persistence and full edge-case hardening explicitly out of scope. If the button moves the save number, it gets properly spec'd next cycle, with this prototype's learnings feeding that spec. This resolves the earlier discrepancy between the PRD (still pending) and Aisling's engineering notes (reported as already resolved via team sync) — Carlos's direct written answer is authoritative |
| 2026-09-16 | A successful save suppresses only the First-Save Moment prompt; the persistent button and tooltips remain visible, unaffected by save state | Team, at engineering sync | The prompt is a one-time nudge; the button and tooltips serve an ongoing, independent role regardless of whether the user has already saved once |
| 2026-09-16 | First-visit definition finalized: dedicated local `excalidraw-c1` record determines first-time status, not autosave presence. Private/incognito = new visitor. Multiple tabs share storage and a `storage` event listener suppresses duplicate prompts across tabs | Team, at engineering sync | Autosave writes within seconds, so without a dedicated record a same-session reload would look like a returning visitor and the prompt would never fire |
| 2026-09-16 | Event vocabulary adopted: `session_start`, `first_element_placed`, `prompt_shown`, `prompt_dismissed`, `save_action` (fields: `method`, `entry_point`), `plus_link_click`. A save counts only when it succeeds — never on `AbortError` | Team, at engineering sync | Existing `trackAction` fires before a save completes, which would over-count canceled saves; success-gating fixes this. One shared vocabulary serves the prompt, button, and tooltips alike. **Note 2026-09-18: `plus_link_click` now has no event source, since the Plus link is excluded from the prompt entirely. Resolved 2026-09-18: event removed — see below** |
| 2026-09-16 | Accessibility acceptance criteria finalized at WCAG 2.1 AA with testable pass conditions — contrast (4.5:1 text / 3:1 controls), 44×44px targets, scoped Escape behavior (dismisses only when focus is inside the prompt; visible ✕ always works), single polite screen-reader announcement, tooltip keyboard behavior | Team, ratified by Jill and Carlos | Closes the previously under-specified accessibility gap with concrete, checkable criteria instead of directional intent (R12) |
| 2026-09-16 | Copy ownership confirmed: Lawrence writes prompt copy and screen-reader announcement text as one task; tested in Jill's Round 0 Lyssna study before the stage 1 build is finalized | Team | Copy-writing and accessibility wording are the same underlying task; Lawrence's existing scope (copy review, client correspondence) made him the natural single owner |
| 2026-09-16 | Failure states and rollback procedure adopted as specified — storage unavailable, analytics failure, canceled/failed saves, unconfirmable downloads (Firefox/Safari), Excalidraw+ link failure; three independent kill switches; rollback triggers tied to the six guardrails plus named hard defects (prompt on empty canvas, shown to signed-in user, re-shown after dismissal, shown twice in one profile, feature-traced error spike) | Team, at engineering sync | Closes a previously undocumented gap; rollback ties directly to guardrails already in the PRD rather than inventing new criteria. **Note 2026-09-18: the Excalidraw+ link failure state no longer applies since the link was removed from the prompt** |
| 2026-09-16 | No PRs opened until the PRD reflects all of the above | Team | Keeps the repo clean and avoids building against numbers that were still in flux the same evening. Lifted once the PRD update lands |
| 2026-09-17 | If Carlos's mockup approvals slip and a stage must be cut to hit the deadline, Stage 3 (tooltips) drops first | Jillian, in response to builder PRD feedback | Tooltips are the lowest-build-risk surface to lose last-minute; dropping them protects the prompt and persistent save button, which carry the primary save-metric impact |
| 2026-09-18 | Card design "Option E" approved — headline, body copy, single "Save to file" primary action, dismiss control; no Excalidraw+ link shown in this version | Carlos, in meeting | Reviewed against the Stage 1 composite; matches the tested no-disclosure copy direction |
| 2026-09-18 | **Excalidraw+ link resolved: not included in the C1 prompt.** Closes the open question flagged since 2026-09-16 (file-save-first / Plus-link-second ordering, conditional on staying subordinate and not cannibalizing saves) — the link is dropped from the prompt entirely rather than ordered second | Carlos, in meeting | Direct decision at the 1pm check-in; supersedes the 9/16 conditional entry rather than deleting it |
| 2026-09-18 | **Supersedes the 2026-09-18 positioning-spec entry (Aisling's x:228/y:76 recommendation).** Card position direction changed from the original recommendation (left of canvas, adjacent to the shape-properties panel) to **right side of canvas** (upper-right or lower-right, below/above the existing button clusters). Exact side (upper vs. lower) and exact coordinates were **not yet decided as of this entry** — see the 9/18 A/B-read entries below for how this was actually resolved | Carlos, in meeting | Client preference stated directly in the 1pm check-in; supersedes rather than deletes the prior spec per team convention — original measurements remain valid reference data for panel/toolbar bounds even though the card's own position has moved |
| 2026-09-18 | New Lyssna test added: positional A/B read, upper-right vs. lower-right placement, requested explicitly by Carlos as an early directional signal before committing to final coordinates | Carlos, in meeting | Carlos flagged this as open and wants early read before Aisling locks in build specs |
| 2026-09-18 | Lower-right position has no existing measured spec. Team will request a quick visual approximation from Aisling for both upper-right and lower-right (not full DevTools-measured specs) to unblock the Lyssna A/B test; full measured spec deferred to whichever position wins the read | Team | Avoids Aisling doing precise measurement work twice for a position that may not be selected |
| 2026-09-18 | Dwell time held at 15s, not re-confirmed at this value by Carlos today — the 2026-09-16 flag (originally ~30s per Carlos, revised to 15s by the team) remains open and unresolved | Carlos, in meeting (no change made) | Explicitly deferred rather than decided; still needs direct re-confirmation from Carlos at some point. **Superseded later the same day — see final dwell row below** |
| 2026-09-18 | **Both A/B test positions measured and built as real stimuli, not rough boxes.** Position A (upper-right): x 1036, y 68, 340×141, right edge aligned to the Excalidraw+/Share/sidebar cluster, 16px below it. Position B (lower-right): x 1036, y 691, same size, 16px above the help/encryption buttons. Both rendered using the approved Option E card (real copy, shadow, colors) directly in the running app — usable as Lyssna test stimuli as-is | Aisling, measured against the running app | Same rigor as the original left-side spec; clean unannotated versions built for the Lyssna stimulus, separate annotated copies exist for internal Slack discussion per the file's own convention |
| 2026-09-18 | Card height confirmed at exactly 141px rendered — matches the ~141px estimate exactly, no variance | Aisling, measured against the running app | Closes the "don't force it" caveat from the handoff; height remains auto/hug-contents in the component, this is just the resulting rendered value for this copy |
| 2026-09-18 | Card visual spec should reference Excalidraw's own design tokens (`var(--ui-font)`, `var(--color-primary)`) rather than literal hex values, once real component work begins. Currently identical values (`Assistant, system-ui...` / `#6965db`), so no visual change today — but using tokens means theming and future brand changes propagate automatically | Aisling | Mockups intentionally kept literal hex so they match the approved export exactly; the real build should use tokens |
| 2026-09-18 | Dark-theme values needed for the card — currently unspecified (white background, #1B1B1F headline, #7A7A7A body only). Required by the team's own ratified accessibility criteria (PRD §6a, 4.5:1 contrast in both themes). **Resolved: Aisling to derive dark-theme values from the app's existing surface tokens** rather than the team specifying new custom colors | Aisling flagged the gap; Jillian decided the resolution approach | Keeps the card visually consistent with Excalidraw's own dark theme rather than introducing one-off colors; avoids a second design pass later |
| 2026-09-18 | Position A/B read (upper-right vs. lower-right) runs as two separate first-click tests, between-subjects, not a preference test | Jillian | The question is whether people find and act on the prompt in each spot, which is behavioral. A preference test measures taste and can pick a winner people still miss. Separate tests keep each participant to one position so the second view is not contaminated. Requested by Carlos (see row 44). |
| 2026-09-18 | Task wording deliberately omits the word "save": "You want to keep this drawing so you don't lose it. Where would you click?" | Jillian | The card itself says save, so the PRD's Stage 1 wording ("where would you click to save it") would point participants straight at it and inflate success in both positions. Deviates from PRD §9 Stage 1 wording. |
| 2026-09-18 | Screeners: (1) online whiteboard experience, all answers qualify, tagged for analysis; (2) "Before today, has anyone shown you designs for a save prompt on a whiteboard app?" with Yes = disqualify | Jillian | Experience is tagged, not filtered, so ceiling effects can be checked by group. Prior exposure is the main bias risk with classmates. Product name left out of both to avoid priming and confusion for non-Excalidraw users. |
| 2026-09-18 | Both stimuli resized to identical dimensions 1229×765 (proportional scaling); originals kept | Jillian | Originals rendered larger than a laptop viewport and B's card sat below the fold, which would bias results toward A. Card, copy, colors, and position unchanged. Files renamed neutrally (stimulus-1 / stimulus-2) so the name cannot reveal position to participants. |
| 2026-09-18 | Results counted by hand from the click map: success = click on Save to file; partial = elsewhere on the card; miss = anywhere else. Boundaries set before any responses | Jillian | Lyssna has no pre-defined success area for this setup. Fixing the rule first keeps the read honest. The follow-up poll on interference is a soft signal only (static image). |
| 2026-09-18 | Read is a directional signal from a focused sample, not a decision. Sample is classmates and friends (skews design-fluent) plus an optional panel top-up with free credits. Classmate and panel results reported separately | Jillian | Consistent with PRD §9 framing. Does not replace the +3pp A/B on Excalidraw's infrastructure. A tie is a likely outcome at this sample size, so Carlos or Aisling breaks it. |
| 2026-09-18 | Position A/B Lyssna study launched (two first-click tests, upper-right vs. lower-right, between-subjects). This is the start point for the read | Jillian | Documents the launch so results can be dated against it and against any later stimulus changes |
| 2026-09-18 | Dwell time confirmed at 15s during the 1pm meeting. Closes the flag open since 2026-09-16 (Carlos originally said ~30s; team moved to 15s). Supersedes the earlier 9/18 row that recorded dwell as "held, not re-confirmed": that row reflected the state at the start of the meeting, and Carlos confirmed later in the same meeting | Carlos, in meeting | Direct confirmation of the team's value, so no further flagging needed |
| 2026-09-18 | `plus_link_click` removed from the event vocabulary and the spec. Closes the open item. Supersedes the "defined-but-dormant vs. remove" question raised in the 9/16 event vocabulary row note | Jillian | The Plus link no longer exists in the prompt, so the event has no source. A dead P0 event in the spec would confuse QA and inflate the instrumentation checklist. The remaining vocabulary is unchanged: `session_start`, `first_element_placed`, `prompt_shown`, `prompt_dismissed`, `save_action` |
| 2026-09-18 | Card body text and dismiss control use `--color-gray-70` (#5c5c5c, 6.69:1 light), not the bare #767676 that Aisling first proposed. Closes the "#7A7A7A fails WCAG AA at 4.29:1" open item | Aisling; Carlos to be informed at the Monday demo review, not asked | Token clears AA with room (#767676 clears by 0.04) and flips correctly in dark theme. Enforcing the WCAG 2.1 AA requirement Carlos already ratified in PRD §6a, so it is not a new design decision. The build already uses the token, so the spec is catching up to the code |
| 2026-09-18 | Dark-theme values for the card measured on the real render and passing AA. Closes the "dark theme values do not exist" open item. Card background `--island-bg-color` #232329; headline `--text-primary-color` #e3e3e8; body and dismiss `--color-gray-40` #b8b8b8; button `--color-primary` #a8a5ff; button text `--color-surface-lowest` #121212. Ratios: 12.22 / 7.88 / 8.47 / 7.88 | Aisling, measured; approach decided by Jillian | Excalidraw's gray scale does not flip between themes, so the light-theme grey measured 2.34:1 on a dark card. `--color-primary` becomes light lavender in dark mode, which made white button text 2.21:1. Both were found only by rendering in dark |
| 2026-09-18 | Card position implemented as a named config anchor (`upper-right` / `lower-right`), with `?pos=lower-right` as a dev override. Trigger logic contains no coordinates | Aisling | Either result of the Lyssna read is a one-line change, so the build is not blocked on the read |
| 2026-09-18 | Shipped card sits at the app's standard 16px gutter, so its right edge is about 48px further right than in the Lyssna stimuli (which were aligned to the button cluster). Recorded so the results are read with this in mind | Aisling | Expected to be immaterial to an upper-vs-lower read, but the shipped position is not pixel-identical to what participants clicked |
| 2026-09-18 | Position B stimulus resized so the card sits above the fold, originals kept. Logged here as Aisling's confirmation of Jillian's earlier resize row | Aisling (confirming Jillian's call) | Below-the-fold would have biased the read toward A, since participants cannot click what they cannot see |
| 2026-09-18 | PRs #2 (this file) and #6 merged to `main`, all checks green | Aisling | Record of merge state |
| 2026-09-18 | Known flake: the MermaidToExcalidraw snapshot test failed the Node 20 job on a docs-only PR and passed on re-run with no change. Inherited from upstream and sits inside a required check. If a PR goes red on it, re-run before investigating | Aisling; Cornell to be informed | Prevents time spent chasing a red check that is not caused by our work |
| 2026-09-20 | **Persistent save button label is "Save to file"**, matching the card (Option E). Visible text, `aria-label` and tooltip change together in one commit. Composite to be regenerated. Lawrence to be informed | Jillian | Adjacent controls with near-identical copy read as a mistake. Carlos approved the card copy on 9/18, so the unapproved button changes. Visible text and accessible name must match (WCAG 2.5.3). Button widens from 71px to about 82px and the cluster still fits |
| 2026-09-20 | **Button styled to match Excalidraw+ using the same tokens:** `--color-surface-low` at rest, `--color-primary` on hover, `--color-primary-darker` on press. Text button, 36px tall like its neighbors. Real `<button>` with a visible focus ring. Supersedes the white-fill outline in composite v1. Measured (light / dark): label on fill 14.61 / 12.22; hover fill vs canvas 4.68 / 8.47; press 5.63 / 9.27; focus ring 5.91 / 10.18 | Aisling (built, measured); Jillian (direction) | Excalidraw+ already turns purple on hover, so v1's worry about two primary buttons was wrong. All ratios reproduced independently |
| 2026-09-20 | **Resting style accepted as is.** Resting fill measures 1.18:1 (light) and 1.20:1 (dark) against the canvas, below the 3:1 in WCAG 1.4.11 and PRD §6a. Same fill as Excalidraw+, so it is inherited. Deliberate deviation. Border-only and darker-fill options not pursued. Carlos to be told | Jillian | Matches the product's own style and keeps a secondary action from outranking Share. The label stays legible in every state. Carlos ratified §6a on 9/16, so a known exception should be said out loud |
| 2026-09-20 | **Target size:** the button matches its neighbors (36px tall) and is held to the AA minimum (24×24, WCAG 2.5.8). PRD §6a's 44×44 line applies to the card's controls only | Jillian | Already passes AA at 36px. 44px would make it taller than Share and Excalidraw+. Desktop only this cycle |
| 2026-09-20 | **Carlos sees screenshots only:** button at rest, button on hover, and the save card open below the cluster (position A), light theme. No hex codes, tables or notes. Labeled as a preview from an unmerged branch. v2 stays internal | Jillian | He needs the design, not the measurement breakdown |
| 2026-09-20 | **Demo scope: Feature 1 (prompt) only.** The button is a screenshot preview, not live and not merged. The bundle stays the tested treatment (PRD §4); the demo arm shows the prompt only until Stages 2 and 3 merge. The prompt and button branches don't merge cleanly (`App.tsx`, two hunks); Aisling resolved it locally, unreviewed. Stage 2 still needs mockup approval, then Gate A | Jillian; Aisling (merge status) | Keeps the demo honest about what is merged. The button was built before its mockup gate |
| 2026-09-20 | **`?arm=control\|nudge` is live and verified on the deployed link.** Control hides the card, nudge shows it. Sticky across reload. Demo badge shown. No console errors. Demo build only. Carlos's email gets one added line: flip between arms to compare; the choice sticks until changed | Aisling | Closes the P1 demo switch in PRD §3. Because it's sticky, Carlos must flip it deliberately |
| 2026-09-20 | **Storage-unavailable behavior is built and tested.** If the browser can't persist the record, the prompt doesn't show | Aisling | Confirms PRD §6b's first row. No need to ask Cornell |
| 2026-09-20 | **Same-business-day turnaround** for Carlos's approvals. Supersedes the open turnaround item and the PRD §7 risk row. The class requires more email and Slack outreach, so extra short check-ins that ask for something specific are fine | Team (turnaround agreed with Carlos) | Weekend availability was not stated, so nothing is logged on it |
| 2026-09-20 | **Screen-reader announcement wording for the card approved.** Closes the PRD §12 item. The final text lives in PRD §6a | Lawrence (approval reported by Jillian) | Must match the on-screen card copy exactly (PRD §6a) |
| 2026-09-20 | **Target save rate among drawers is 41.7% (25 of 60), not ~40.6%.** 22% activation over the 60% who draw is 36.7%; +3pp overall is 25/60. ~40.6% was an arithmetic slip with no method behind it. PRD §2c and build brief §4.1 to be corrected | Jillian; Aisling confirmed the slip | Carlos will do this math for his board |
| 2026-09-20 | **Sample sizes verified** (two-proportion, two-sided, 95% confidence, 80% power): 3,134 per arm for +3pp (22% to 25%); 5,760 per arm for the 10% relative framing (22% to 24.2%). Totals about 6,300 and 11,500. Supersedes ~3,150 and ~5,900 in draft agenda text | Aisling (calculated); reproduced independently | Carlos uses these for his board. The build brief's ~3,100 and ~5,800 are consistent |
| 2026-09-20 | **Plus link removal applies to the card only.** The existing Save dialog, Export dialog and menu, including their Excalidraw+ options, are unchanged Excalidraw interface | Jillian | Clarifies the 9/18 decision. Check any future surface that routes to those dialogs against it |
| 2026-09-20 | **Ownership:** Aisling took over the persistent button from Cornell. Cornell keeps telemetry and events | Team | Records the change. The QA sign-off owner for the button is still to confirm |
| 2026-09-21 | **Card position: upper-right confirmed.** First-click read: 8 of 12 on the Save button (upper-right) vs. 7 of 14 (lower-right), hand-scored. The gap is inside noise at this sample, so the choice is directional. Position stays a named config anchor and will be revisited against live A/B data. Closes the position item in Still open | Carlos, in client meeting | Upper-right leads and sits with the existing top-right actions. The test used a static image, so participants could not know the card appears once and goes away. Live dismissal and exit rates are the better evidence. Both tests were still recruiting when read (under the 15 to 20 target) |
| 2026-09-21 | **Interference follow-up treated as non-informative.** "Somewhat" or "A lot" in the way was similar across positions | Jillian | Same static-image limit: participants could not tell the card is one-time and dismissible. PRD §9 already calls this a soft signal |
| 2026-09-21 | **Menu-area misses noted.** About half of misses in both tests were clicks toward the top-left menu, regardless of card position | Jillian | Supports keeping the persistent save button as a second surface. Observation only, not a separate decision |
| 2026-09-21 | **Stage 1 card approved to merge to main.** Screen reader, keyboard, dark theme and storage-unavailable behavior reported as tested on the card | Carlos, in client meeting | Approval covers the card. The button, picker and tooltips are separate. Card body text and dismiss control use `--color-gray-70` (9/18 row); this was disclosed at the meeting |
| 2026-09-21 | **The card and the persistent "Save to file" button both open a format picker (Excalidraw file, PNG image, SVG image), and the chosen format downloads directly with no system save dialog.** Supersedes the 2026-09-16 row and PRD §6 (button saves directly, no Save dialog). The existing Save and Export dialogs are unchanged. Share link and Excalidraw+ are not in the picker. Merging save and export into one modal was raised and deferred | Carlos, in client meeting (format choice); Aisling (build) | Carlos wants people to save in the format they choose, with choices shown before any download. Direct save stays an acceptable baseline. This adds a step before a save completes and is a preference-based call, not a measured one. The live A/B should show whether it costs completed saves. Carlos has seen the picker; his confirmation is pending |
| 2026-09-21 | **Stages 1 and 2 merged to main and live on the demo link, tag `g1-v0.2.0-beta.1`.** 71 app tests pass, CI green. `?arm=control` hides both surfaces, `?arm=nudge` shows both. Supersedes the 2026-09-20 demo scope row (prompt only, button as screenshot). The 3-shot composite for Carlos is replaced by the live build | Aisling | Record of merge state. The button was previously an unmerged preview |
| 2026-09-21 | **Picker wording is Aisling's draft, pending Lawrence's approval.** "Excalidraw file: Open it again later and keep editing"; "PNG image: A picture to share or paste anywhere"; "SVG image: Sharp at any size, for the web or print" | Aisling (draft); Lawrence to approve or rewrite | Lawrence owns prompt copy. Approval also needs to cover whether the card's approved screen-reader text still fits now that the card opens a picker |
| 2026-09-21 | **Telemetry rule for the picker:** `save_action` fires when a picker download succeeds, not on the click | Aisling | Consistent with the 9/16 event row (a save counts only on success). Cornell keeps telemetry |
| 2026-09-21 | **Button resting outline disclosed to Carlos.** Resting fill is 1.18:1 (light) and 1.20:1 (dark) against the canvas, below the 3:1 in WCAG 1.4.11 and PRD §6a. Label and hover pass. Kept matched to the Excalidraw+ button so it does not compete with Share. Carlos was offered a stronger look and has not asked for one | Aisling (disclosure) | Closes the 9/20 "Carlos to be told" item. The deviation stays deliberate unless he asks for a change |
| 2026-09-21 | **Sample sizes stated as 3,135 and 5,761 per arm** (rounded up from 3,134 and 5,760) | Aisling | Whole sessions, and the safer figure for Carlos's board. Totals stay about 6,300 and 11,500 |
| 2026-09-21 | **Tooltips (Stage 3) remain open**, pending review time before submission. Stage 3 stays first to cut if approvals slip (9/17) | Team | Consistent with the 9/17 cut order |
| 2026-09-21 | **Handoff to Pursuit's internal engineers targeted for Wednesday.** Review session to be set for tomorrow or Wednesday | Team | Sets the deadline for the open items below |
| 2026-09-22 | **Format picker restyled to match the main menu's "Save to..." dialog**, built from the same `Card`, icon and button components rather than a bespoke layout. The Excalidraw file takes that dialog's lime, because it is the same action as its "Save to disk" | Carlos, in the 9/21 review; Aisling (build, PR #13) | Carlos asked that the two read as one family and stay on-brand. Reusing the house components means the picker inherits future changes to that dialog instead of drifting from it |
| 2026-09-22 | **Any completed save clears the card, from either surface.** Supersedes the behaviour where only the card's own save cleared it, and closes PRD §5. Implemented as a shared in-page save signal, not a storage listener | Jillian (PRD §5) and Carlos both asked; Aisling (build, PR #13) | The PRD always said any successful save suppresses the card, so the old behaviour was a bug against it, not a design choice. A `storage` event never fires in the tab that writes it, so cross-tab suppression alone could not clear the card in the tab doing the saving |
| 2026-09-22 | **Picker card text contrast corrected against Excalidraw's own defaults.** White on the lime card measures 2.44:1, so it uses `--color-gray-100` (#121212, fixed in both themes) at 7.7:1. White on the primary card falls to 2.21:1 in dark mode, so it uses `--color-surface-lowest`, which flips with the theme | Aisling (measured and built) | A deliberate deviation _from_ the house style in order to meet AA: the main menu's own "Save to..." dialog does not pass at these combinations. Matching it exactly would have shipped a known failure |
| 2026-09-22 | **The demo build's "Shareable link" card is present but inert**, with the reason written on the card rather than hidden in a tooltip. Demo build only | Carlos (asked for it greyed out); Aisling (build, PR #13) | Gated on `IS_DEMO_BUILD` (`VITE_APP_IS_DEMO`, set only in `.env.demo`, loaded only by `vite build --mode demo`, run only by the Pages workflow); the demo separately blanks `VITE_APP_BACKEND_V2_POST_URL` and passes `onExportToBackend: undefined`. Normal builds keep the stock Save dialog, so §8's share-link guardrail is unaffected |
| 2026-09-22 | **Lawrence's picker copy approved and shipped verbatim:** "Excalidraw file - Keeps your shapes editable."; "PNG image - A flat image for sharing."; "SVG image - A flat image that stays sharp at any size."; empty canvas - "Draw something first." | Lawrence (approval); Aisling (build, PR #14) | Closes the picker-wording item. The card promises "keep working with it later" and only the Excalidraw file delivers that, so the distinction is now stated on the card that makes the promise |
| 2026-09-22 | **"Recommended" ships as a badge, not as the first word of the description** | Aisling (judgement call); Lawrence (confirmed 2026-09-23: "badge alone, not repeated in the sentence. Aisling's call was right") | Saying it in the badge and again in the sentence repeats it on one card. Raised with Lawrence the same evening and confirmed by him the next day |
| 2026-09-22 | **The badge sits above the icon, and the other two cards carry an `aria-hidden` twin of it** to hold the same height | Jillian (placement); Aisling (build) | Below the icon, the badge pushed its own column's icon, title and button out of line with the other two. Measured on the live render: icon, title, description and button share a row across all three columns; badge contrast 7.48:1, and both its colours are fixed so it holds in dark mode. The twins are hidden from assistive technology so "Recommended" is announced once, not three times |
| 2026-09-22 | **Two releases tagged and verified live:** `g1-v0.2.1-beta.1` (picker restyle, card clearing, inert demo share-link card) and `g1-v0.2.2-beta.1` (Lawrence's copy, Recommended badge). `g1-v0.2.0-beta.1` in PRD §13 is two releases stale | Aisling | Anyone checking out the tag named in §13 gets the old picker, none of Lawrence's copy and no badge |
| 2026-09-22 | **Gate A picker screenshots refreshed** to match the shipped copy and badge (PR #15). The previous captures predated PR #14 | Jillian (asked); Aisling (recaptured) | The accessibility evidence referenced by §6a no longer matched the build going to handoff |
| 2026-09-22 | **Combined save/export stays a V2 proposal.** Mockup rounds 2, 3 and 4 filed under `docs/cycle-2/roadmap/combined-save-export/` with `CSE-01-` naming and an Appendix D entry | Jillian (folder, naming, Appendix D); Lawrence (scope caution); Aisling (mockups) | Keeping them out of the handoff stops anyone reading them as open items. The folder sits inside the git-excluded team-docs path, so files need a force-add, as the evidence folder already does |
| 2026-09-23 | **Chooser conformed to the house "Save to..." dialog: colour order and card widths** (PR #18). Colours now run lime, pink, violet as that dialog does; ours ran lime, violet, pink. All three cards fill their column, where ours measured 207 / 180 / 207 against the house dialog's 207 / 207 / 207 | Carlos (requirement, 2026-09-23); Aisling (measured and built) | Measured both dialogs on the build Carlos tested before changing anything: island padding and radius, the card grid, card padding and margins, icon size, heading and description type, and button height, radius, padding and font were already identical. The width gap was not a styling difference - the card is shrink-to-fit and the house dialog's descriptions all happen to fill their column |
| 2026-09-23 | **The one point of Carlos's conformance requirement deliberately not met: the lime button's label stays near-black (7.68:1) rather than the house dialog's white (2.44:1).** Disclosed to Carlos with the numbers rather than changed silently in either direction | Aisling (call); Jillian (agreed in huddle, 2026-09-23, and to document the deviation and rationale in the handoff) | Conforming exactly would put a known WCAG AA failure into the surface the cycle is measured on. Everything else about that button already matches the house dialog exactly |
| 2026-09-23 | **The chooser forces `exportEmbedScene` off, so its image saves are always flat** (PR #19). **Provisional pending Carlos.** Measured on the deployed build with Embed scene on: SVG 1,092 to 2,197 bytes and PNG 7,057 to 9,699 bytes, each carrying the whole editable scene | Jillian (fallback call); Aisling (found, measured, built) | "Embed scene" is a per-browser setting that sticks once someone turns it on in the Export image dialog, and both exporters honour it, so the chooser was handing that person a file its own cards call flat. Forcing it off keeps approved copy true and needs no new sign-off; the alternative - inherit and rewrite the cards - needs Carlos and Lawrence. Only the chooser is overridden; the Export image dialog is a separate path, so anyone who wants an embedded image still has one. One line to reverse |
| 2026-09-23 | **The Recommended badge is named in the button's accessible name**, as "Save as Excalidraw, recommended", while the visible label stays "Save as Excalidraw" | Lawrence (proposed the fix); Aisling (built) | Tabbing the picker announced three options that sounded equivalent, so the recommendation reached people reading the dialog as text but not people navigating by control. Implemented as the intent rather than the literal suggestion: `showAriaLabel` renders the accessible name as the visible label, so setting `aria-label` alone would have changed the button's on-screen text too. The visible label is rendered separately and the accessible name contains it, which keeps WCAG 2.5.3 |
| 2026-09-23 | **Picker wording is closed, not open.** What Lawrence approved on 9/22 was the format copy inside the picker; the button label was approved separately on 9/20 | Lawrence, correcting the record 2026-09-23 | Recorded because the handoff list had it as still blocking. It is not |
| 2026-09-23 | **Tooltips (Stage 3) moved to the backlog** rather than built or formally cut. Supersedes the 9/21 "remain open" row and resolves the contradiction with the roadmap §3 line that said they were cut | Team, 2026-09-23 | Three sources carried two states. Backlogged is the honest description: not built this cycle, not ruled out for a later one |
| 2026-09-23 | **Canonical documents named:** the off-repo PRD Jillian maintains, and this file for decisions. The 9/18 local snapshots under `docs/cycle-2/prd/` stopped updating at 2026-09-18 18:15 and disagree with both | Jillian and Aisling | Two decisions files and two PRDs were in circulation; an engineering flag was raised against a four-day-old copy as a result. Deletion of the snapshots is pending Jillian |
| 2026-09-23 | **One live URL for the submission, not two.** Control versus treatment is the `?arm=` switch on the single demo link: `?arm=control` hides both surfaces, `?arm=nudge` shows both. Before versus after a fix is Gate A evidence, carried by the tags plus stamped screenshots 44 to 47. A second deployment was declined as new infrastructure | Jillian (call); Aisling (verified) | Nothing in the PRD or this file asks for two live URLs, and PRD §5 only asks for before/after screenshots. Both arms re-verified on the deployed build, including that the choice sticks across a reload — which is why the bare link shows whichever arm was last chosen and must not be sent on its own. Carlos gets both `?arm=` URLs spelled out, with a note that the choice is sticky |
| 2026-09-23 | **Focus returns to the persistent save button after a save from the card** (PR #21, tag `g1-v0.2.4-beta.1`). Supersedes the fallback shipped in #16, which never ran | Aisling | `onSaved` and `onClose` are state setters, so the first attempt checked whether the opener was still connected before React had re-rendered: it focused the card's button every time and focus fell to `<body>` when React removed it. The decision now happens after the commit. Caught by re-running the accessibility evidence against the deployed build and finding `<body>` on a build that supposedly contained the fix; the unit test had passed because it removed the opener synchronously, which the real app never does |
| 2026-09-23 | **Releases tagged and verified live:** `g1-v0.2.3-beta.1` (house colour order, equal card widths, flat image saves, accessible-name fixes) and `g1-v0.2.4-beta.1` (the focus correction). PRD §13 should name `g1-v0.2.4-beta.1` | Aisling | Verified on the deployed build in both themes: colour order, equal widths, label contrast 7.68 / 4.62 / 4.68 light and 7.68 / 4.62 / 8.47 dark, images flat with Embed scene on, accessible name "Save as Excalidraw, recommended" against visible label "Save as Excalidraw", focus returning after both save paths, and both `?arm=` switches. No page errors |
| 2026-09-23 | **PR #17, the upstream dialog accessible-name defect, is held rather than merged.** The patch and its tests stay open and attached | Aisling (proposed); Aisling and Jillian (held), pending a scope call | Every dialog in the app is announced without a name, because `Dialog.tsx` points `aria-labelledby` at an id that is never rendered. The fix is one line, but it is in `packages/`, which Carlos placed out of scope, and merging widens this fork's diff against upstream Excalidraw. Recorded as a known upstream defect with the patch attached so whoever picks it up does not have to rediscover it |

---

## Still open

| Question | Owner | Needed by |
| :-- | :-- | :-- |
| Carlos's confirmation of the picker (Excalidraw file, PNG, SVG) and of both surfaces opening it. He has seen it; reply pending | Carlos | Before handoff |
| Carlos: whether the chooser should force Embed scene off (shipped provisionally, PR #19) or inherit it and have the cards rewritten. A 6pm cutoff was set internally; no reply means the provisional behaviour stands and is recorded as provisional, not approved | Carlos | Before handoff |
| Carlos: the Excalidraw+ card's Save/Export label inversion. He called it a fix that "should have happened regardless", but that card sits in the "Save to..." dialog he placed out of scope in the same message. Our read, on the 9/20 row, is that it stays out and joins the fast-follow list | Carlos | Before handoff |
| Carlos: the reopen line on the Excalidraw card. Wording from Lawrence, after the correction that the menu's Open (Ctrl/Cmd+O) is the route rather than drag-and-drop | Carlos, then Lawrence | Before handoff |
| Who signs off the picker accessibility review. PRD §10 assigns QA sign-off to Cornell; Lawrence offered, having already tested in VoiceOver. §12 needs a name | Cornell or Lawrence | Before handoff |
| Ctrl/Cmd+O end to end, by hand, in Chrome, Firefox and Safari. The menu item and the shortcut registration are both confirmed, but a scripted test cannot fire a file picker, so this needs a person - and not the builder, per the 9/20 row | Unassigned | Before handoff |
| Whether the picker raises or lowers completed saves versus direct save. Needs a picker-opened event and the `method` breakdown in the live A/B | Cornell (event), Carlos (decision on result) | Before the A/B runs |
| Whether to delete the 9/18 local snapshots of the PRD and decisions.md under `docs/cycle-2/prd/` | Jillian | Before handoff |
| The upstream dialog accessible-name defect (PR #17, draft): fix it in `packages/` or record it as a known upstream defect with the patch attached | Team, with Carlos on scope | Before handoff |
| Extra user test (static image with the card and the persistent button, to see which is clicked first). Proposed at the meeting, not scoped | Jillian | Not set |

## Cross-reference

Full requirements, instrumentation spec, accessibility criteria, and failure-state handling live in `persistent-save-master-prd.md`, §§2–9 and §13. This file is the historical/reasoning record; that one is the spec to build against.

**Note:** the PRD and feature roadmap are maintained outside this git repo (kept local/off-repo per the team's stated privacy decision — both documents quote Carlos's brief directly, and this repository is public). This file remains in-repo per team convention, since it's code-adjacent working history without direct client-brief quotations beyond what's already summarized here.
